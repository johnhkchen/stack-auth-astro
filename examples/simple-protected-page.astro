---
/**
 * Simple example demonstrating page protection with requireAuth
 * This page will automatically redirect unauthenticated users to sign-in
 */
import { requireAuth } from '../src/server';

// This line protects the page - unauthenticated users will be redirected to sign-in
const user = await requireAuth(Astro);
// If execution reaches here, the user is authenticated
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Protected Page - Stack Auth</title>
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        max-width: 800px;
        margin: 0 auto;
        padding: 2rem;
        background: #f7f7f7;
      }
      .container {
        background: white;
        border-radius: 8px;
        padding: 2rem;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      }
      h1 {
        color: #333;
        margin-bottom: 0.5rem;
      }
      .success-badge {
        display: inline-block;
        background: #10b981;
        color: white;
        padding: 0.25rem 0.75rem;
        border-radius: 4px;
        font-size: 0.875rem;
        margin-bottom: 1rem;
      }
      .user-info {
        background: #f0f9ff;
        border: 1px solid #0284c7;
        border-radius: 6px;
        padding: 1rem;
        margin: 1.5rem 0;
      }
      .user-info h2 {
        margin-top: 0;
        color: #0284c7;
      }
      .info-row {
        display: flex;
        justify-content: space-between;
        padding: 0.5rem 0;
        border-bottom: 1px solid #e5e7eb;
      }
      .info-row:last-child {
        border-bottom: none;
      }
      .label {
        font-weight: 600;
        color: #6b7280;
      }
      .value {
        color: #111827;
      }
      .code-example {
        background: #1e293b;
        color: #e2e8f0;
        padding: 1rem;
        border-radius: 6px;
        overflow-x: auto;
        margin: 1rem 0;
      }
      .code-example pre {
        margin: 0;
        font-family: 'Courier New', monospace;
        font-size: 0.9rem;
      }
      .back-link {
        display: inline-block;
        margin-top: 1.5rem;
        padding: 0.5rem 1rem;
        background: #3b82f6;
        color: white;
        text-decoration: none;
        border-radius: 6px;
      }
      .back-link:hover {
        background: #2563eb;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <span class="success-badge">🔒 Protected Page</span>
      <h1>Welcome to Your Protected Content!</h1>
      <p>
        This page is only accessible to authenticated users. Since you're seeing this,
        the <code>requireAuth()</code> function has successfully verified your authentication.
      </p>

      <div class="user-info">
        <h2>Your User Information</h2>
        <div class="info-row">
          <span class="label">Display Name:</span>
          <span class="value">{user.displayName || 'Not set'}</span>
        </div>
        <div class="info-row">
          <span class="label">Email:</span>
          <span class="value">{user.primaryEmail}</span>
        </div>
        <div class="info-row">
          <span class="label">User ID:</span>
          <span class="value">{user.id}</span>
        </div>
        <div class="info-row">
          <span class="label">Signed Up:</span>
          <span class="value">{new Date(user.signedUpAtMillis).toLocaleDateString()}</span>
        </div>
      </div>

      <h2>How This Works</h2>
      <p>
        This page demonstrates the simplest way to protect content in Astro with Stack Auth.
        Here's the code used in the page frontmatter:
      </p>

      <div class="code-example">
        <pre>---
import { requireAuth } from 'astro-stack-auth/server';

// Protect this page - redirects unauthenticated users
const user = await requireAuth(Astro);

// This code only runs for authenticated users
---</pre>
      </div>

      <h3>Key Features Demonstrated:</h3>
      <ul>
        <li>✅ Automatic redirect to sign-in for unauthenticated users</li>
        <li>✅ Original URL preserved for post-login redirect</li>
        <li>✅ User data available in page after authentication</li>
        <li>✅ Works with SSR and static generation</li>
      </ul>

      <a href="/" class="back-link">← Back to Home</a>
    </div>
  </body>
</html>