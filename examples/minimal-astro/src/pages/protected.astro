---
import { requireAuth } from 'astro-stack-auth/server';

// This will redirect to sign-in if user is not authenticated
const user = await requireAuth(Astro);
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Protected Page - Minimal Astro + Stack Auth</title>
    <style>
      body {
        font-family: system-ui, sans-serif;
        max-width: 600px;
        margin: 2rem auto;
        padding: 1rem;
        line-height: 1.6;
      }
      .protected-content {
        padding: 1rem;
        margin: 1rem 0;
        border-radius: 8px;
        background: #f0fdf4;
        border: 1px solid #16a34a;
      }
      a {
        color: #0ea5e9;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <h1>🔒 Protected Page</h1>
    <div class="protected-content">
      <p>✅ This page is only accessible to authenticated users.</p>
      <p><strong>Welcome, {user.displayName || user.primaryEmail}!</strong></p>
      <p>This demonstrates the <code>requireAuth()</code> function protecting content.</p>
    </div>
    <p><a href="/">← Back to Home</a></p>
  </body>
</html>